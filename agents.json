{
  "_comment": "Agent registry. Single source of truth for launch-agent.sh, chq-tmux.sh, and the Electron remote. Add a new agent by appending one entry — no new bash file needed.",
  "_field_docs": {
    "id": "Stable lookup key. Used by launch-agent.sh <id>, chq-tmux.sh start <id>, and the Electron remote button id.",
    "display_name": "Value passed to claude's -n <Name> flag. Load-bearing — used by ps-grep detection and pane targeting.",
    "tmux_target": "Pane title used for broadcast + status targeting. Defaults to lowercased rename_to when present, otherwise lowercased display_name. Only set explicitly to override that derivation.",
    "cwd": "Working directory the launcher cd's into before exec. Tilde-expanded by launch-agent.sh.",
    "color": "Argument to claude's /color slash command. Set during the auto-inject sequence.",
    "theme_color": "Hex color string (e.g. '#00f2ff') used by AgentRemote to generate per-agent CSS variables at runtime. Controls the active-border glow and radial-orb palette. Picked in the Add Agent modal. Falls back to --accent if omitted.",
    "rename_to": "Argument to claude's /rename slash command. Defaults to display_name uppercased.",
    "startup_slash": "Slash command auto-injected after boot (default /gogo). Empty string disables.",
    "auto_restart": "If false, chq-tmux.sh's pane_loop exits after the agent quits instead of relooping after RESTART_DELAY. Default true (omit the field to mean true). Editable from the Electron remote's right-click menu.",
    "pre_launch": "Optional path to a hook script run before exec claude. Tilde-expanded. Replaces the inline telegram-cleanup blocks.",
    "env": "Optional map of env vars to export before exec (e.g. KOKORO_VOICE for the telegram-voice-followup hook).",
    "avatar": "Optional avatar SVG filename inside remote-app/assets/. Defaults to <id>.svg if unset. Used by the Electron remote button render."
  },
  "agents": [
    {
      "id": "xavier",
      "display_name": "Xavier",
      "cwd": "~/ai_projects/CorporateHQ",
      "color": "yellow",
      "theme_color": "#00f2ff",
      "startup_slash": "/gogo",
      "pre_launch": "~/ai_projects/CorporateHQ/hooks/telegram-cleanup.sh --pre-launch Xavier",
      "rename_to": "XAVIER"
    },
    {
      "id": "lucius",
      "display_name": "Lucius",
      "cwd": "~/ai_projects/research-and-development",
      "color": "purple",
      "theme_color": "#ffaa00",
      "startup_slash": "/gogo",
      "pre_launch": "~/ai_projects/CorporateHQ/hooks/telegram-cleanup.sh --pre-launch Lucius",
      "env": {
        "KOKORO_VOICE": "am_michael"
      },
      "rename_to": "LUCIUS"
    },
    {
      "id": "gekko",
      "display_name": "Gekko",
      "cwd": "~/ai_projects/trading",
      "color": "green",
      "theme_color": "#00ff88",
      "startup_slash": "/gogo",
      "pre_launch": "~/ai_projects/CorporateHQ/hooks/telegram-cleanup.sh --pre-launch Gekko",
      "rename_to": "GEKKO"
    },
    {
      "id": "swarmy",
      "display_name": "overlordswarmy",
      "cwd": "~/ai_projects/swarmy",
      "color": "red",
      "theme_color": "#ff77cc",
      "rename_to": "OVERLORDSWARMY",
      "startup_slash": "/gogo",
      "pre_launch": "~/ai_projects/CorporateHQ/hooks/telegram-cleanup.sh --pre-launch Swarmy"
    },
    {
      "id": "claude",
      "display_name": "Claude",
      "cwd": "~/agent-launch-scripts",
      "color": "orange",
      "theme_color": "#e07c4c",
      "rename_to": "TMUX-MASTA",
      "startup_slash": "/gogo",
      "avatar": "claude.svg"
    }
  ]
}
