# Session Closeout — 2026-05-21 09:03

## Outcome
Fixed and live-verified the "send not submitting" bug via a two-phase send (root cause: text+Enter fused in one tmux call → TUIs read it as a paste), then carried the same fix into the launcher's startup injection and rebuilt that injection as a runtime-agnostic per-agent toggle. Produced two durable docs (app reference + non-regression spec). 12 commits, all on `main`, not pushed.

## Work shipped
- **Two-phase send/submit fix (v1.4.15)** in AgentRemote (`tmux-send-path.js` + `main.js`): literal text, then a delayed isolated Enter, so Codex/Claude TUIs submit instead of pasting. Live-verified by Richard ("test" → "success"). Integration tests prove 2-reads vs 1-read.
- **Integrated prior dirty work**: codex xhigh reasoning, pane `@agent-identity` owner-matching, Option+Backspace→C-w, registry-priority department resolution, registry-driven pane titles, cleanup script.
- **`docs/agentremote-reference.md`** — current "how AgentRemote works now (v1.4.15)" reference, built from live code.
- **`docs/operations/agentremote-spec-requirements.md`** — non-regression spec (23 requirements, 7 collision clusters, "don't break X to fix Y"), mined from sessions + diffed against code; caught the stale OSC-1337 image-paste note.
- **Startup injection rebuilt as a runtime-agnostic per-agent toggle** in `launch-agent.sh` (commit `4d32e90`): same two-phase fix for injected commands, configurable warning-ack keys ("1 or enter"), Claude-only `/color`/`/rename` never injected into non-Claude panes, argv-vs-injection guard. Runs on every auto-restart. Isolated test green.

## Artifacts
- Commits (12): `32e456f`, `bbeb697`, `e4d2331`, `c43cd4d`, `13732ac`, `805acb6`, `2cf8e1a`, `51fc571`, `8c3052d`, `083113e`, `4d32e90`, `4de9e6a`.
- Detailed folds: `memory/sessions/2026-05-21_0726_send-submit-fix-and-app-docs.md`, `2026-05-21_0836_startup-injection-dynamic-toggle.md`.
- New docs: `docs/agentremote-reference.md`, `docs/operations/agentremote-spec-requirements.md`.
- LEARNINGS: `LRN-20260521-001`. Auto-memory: `feedback_hud_relaunch_not_disruptive.md`.
- HUD relaunched at v1.4.15 (running); live fleet is Claude (Opus 4.7).

## Carryover
- **agents.json** uncommitted (gitignored; matches the live Claude fleet) — commit on Richard's call.
- **/lead-gogo auto-inject gap:** most agents have `startup_slash: /lead-gogo` but NO `startup_injection` policy, so the launcher toggle won't auto-inject the warning-ack + command on restart. To drive startup fleet-wide, add a `startup_injection` policy per agent (e.g. `include: ["dangerous_permission_enter","startup_lines"]`).
- **applyRuntimePolicy** (`remote-app/main.js` ~735, ~2298) strips `startup_injection` from non-Claude agents on UI-edit — lift if HUD-managed Codex injection is wanted.
- **ALS-LOCAL-001** image paste: dedicated live paste check still owed (uses `[image:/path]` text ref, not OSC 1337).
- Nothing pushed; push on request.
