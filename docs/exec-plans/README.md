# Execution Plans

Execution plans are versioned work artifacts for changes that need more than a small inline checklist.

## Layout

- `active/` — plans still guiding implementation or review.
- `completed/` — closed plans kept for decision history.

When a plan closes, move it from `active/` to `completed/` and update `docs/README.md` if it is no longer part of the startup read order.
